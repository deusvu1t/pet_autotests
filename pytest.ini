[pytest]
testpaths = tests

addopts = -v --tracing=retain-on-failure --screenshot=only-on-failure

markers =
    smoke: smoke tests
    regression: regression tests
    ui: ui tests
    api: api tests
